/**
 * Points awarded for a successful wake-up check-in.
 */
export const POINTS_WAKE_UP = 100;

/**
 * Multiplier for streak bonus points.
 */
export const STREAK_BONUS_MULTIPLIER = 10;

/**
 * Maximum daily absolute points allowed (though currently not strictly enforced in accumulation).
 */
export const MAX_DAILY_ABSOLUTE_POINTS = 1440;

/**
 * Key used for localStorage persistence.
 */
export const STORAGE_KEY = 'morning-routine-data';
